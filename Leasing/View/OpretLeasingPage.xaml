<Page
    x:Class="Leasing.View.OpretLeasingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leasing.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>


        <StackPanel Margin="800,825,0,-665">
          

               <ComboBox x:Name="Medarbejder" ItemsSource="{Binding BilIdList}" SelectedItem="{Binding SelectedId}" IsEditable="True" Header="Medarbejder" HorizontalAlignment="Center" Margin="0,0,0,0" Width="500" Height="75">

               </ComboBox>

               <Button Command="{Binding AddCommand}" Name="AddButton" Height="50" Width="100" Margin="0,0,0,0" >
                   <TextBlock Text="Add" Margin="0,0,0,0" ></TextBlock>
               </Button>

            </StackPanel>
                

          


    



    </Grid>
</Page>
